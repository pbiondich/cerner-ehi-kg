# DM_CHG_LOG

> Data Management Change Log

**Description:** DATA MANAGEMENT CHANGE LOG  
**Table type:** REFERENCE  
**Primary key:** _(not published — see note)_  
**Columns:** 26

[← index](../index.md)

## Columns

| # | Column | Type | Null? | Flags | Definition |
|--:|--------|------|:-----:|-------|------------|
| 1 | `BLOCKING_LOG_ID` | DOUBLE | NOT NULL |  | Log ID in target environment's dm_chg_log that caused this row to not be allowed to move. This can only happen when the target is a PTAM (Pending Target As Master) target for this row. |
| 2 | `CHG_DT_TM` | DATETIME | NOT NULL |  | Date and Time the log entry was made |
| 3 | `CHG_LOG_REASON_TXT` | VARCHAR(2000) |  |  | Column to hold reason for particular LOG_TYPE |
| 4 | `COL_STRING` | VARCHAR(4000) |  |  | Column to hold other column values not stored in PK_WHERE |
| 5 | `CONTEXT_NAME` | VARCHAR(256) |  |  | Used to group rows together so that they can be moved together later. It is possible to have the same logical row for the same environment but with different context names. |
| 6 | `DELETE_IND` | DOUBLE | NOT NULL |  | Indicates that data was deleted from the table. |
| 7 | `DM_CHG_LOG_EXCEPTION_ID` | DOUBLE |  |  | A logical Foreign Key to the top-level column on the DM_CHG_LOG_EXCEPTION table |
| 8 | `DM_REFCHG_PKW_VERS_ID` | DOUBLE | NOT NULL | FK→ | Indicates format that the pk_where column uses. |
| 9 | `LOG_ID` | DOUBLE | NOT NULL |  | identifier for log entry |
| 10 | `LOG_TYPE` | VARCHAR(6) | NOT NULL |  | Type of item logged. (i.e. REFCHG for reference data changes.) |
| 11 | `PK_WHERE` | VARCHAR(2000) | NOT NULL |  | Where clause to qualify using primary key of the table involved. |
| 12 | `PK_WHERE_HASH` | DOUBLE |  |  | Hash value of PK_WHERE template |
| 13 | `PK_WHERE_VALUE` | DOUBLE | NOT NULL |  | Where clause to qualify using primary key of the table involved that has been encoded. |
| 14 | `PTAM_MATCH_HASH` | DOUBLE |  |  | Hash Value of PTAM_MATCH_TEMPLATE |
| 15 | `PTAM_MATCH_QUERY` | VARCHAR(2000) |  |  | Column to hold the query that PTAM logic should use to find a blocking row |
| 16 | `PTAM_MATCH_RESULT` | DOUBLE |  |  | Column to hold the result that PTAM logic should use to find a blocking row. The PTAM_MATCH_RESULT will just be a numerical value that comes from a string being run through the oracle hash function. |
| 17 | `PTAM_MATCH_RESULT_STR` | VARCHAR(4000) |  |  | This column will hold the string generated by the PTAM_MATCH_QUERY function whose hash value is already stored in PTAM_MATCH_RESULT |
| 18 | `RDBHANDLE` | VARCHAR(30) |  |  | Records the CURRDBHANDLE that inserts/updates the row into this table. |
| 19 | `SINGLE_PASS_LOG_ID` | DOUBLE |  |  | Column to hold the DM_CHG_LOG.LOG_ID value that required this row to be added to the DM_CHG_LOG. |
| 20 | `TABLE_NAME` | VARCHAR(30) | NOT NULL |  | Table name of changed table. |
| 21 | `TARGET_ENV_ID` | DOUBLE | NOT NULL |  | environment id of target database |
| 22 | `UPDT_APPLCTX` | DOUBLE | NOT NULL |  | The application context number from the record info block. The UPDT family of columns are typically used for housekeeping and external system process and should never be depended on for solution specific logic. |
| 23 | `UPDT_CNT` | DOUBLE | NOT NULL |  | Set to 0 on insert. Incremented by 1 on update. Used to recognize update conflict where data in a row updated by one application is at risk of being lost by a second application attempting to update the row. The UPDT family of columns are typically used for housekeeping and external system process and should never be depended on for solution specific logic. |
| 24 | `UPDT_DT_TM` | DATETIME | NOT NULL |  | The date and time the row was last inserted or updated. The UPDT family of columns are typically used for housekeeping and external system process and should never be depended on for solution specific logic. |
| 25 | `UPDT_ID` | DOUBLE | NOT NULL |  | The person_id of the person from the personnel table (prsnl) that caused the last insert or update of the row in the table. The UPDT family of columns are typically used for housekeeping and external system process and should never be depended on for solution specific logic. |
| 26 | `UPDT_TASK` | DOUBLE | NOT NULL |  | The registered (assigned) task number for the process that inserted or updated the row. The UPDT family of columns are typically used for housekeeping and external system process and should never be depended on for solution specific logic. |

_Flags: PK = primary key · FK→ = published foreign key (see Joins out)._

## Joins out — this table references (published FKs)

| Column | → References | Parent column |
|--------|--------------|---------------|
| `DM_REFCHG_PKW_VERS_ID` | [DM_REFCHG_PKW_VERS](DM_REFCHG_PKW_VERS.md) | `DM_REFCHG_PKW_VERS_ID` |

